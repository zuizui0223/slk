# SLK theorem-claim ledger V1

This ledger separates proved theory, model-specific corollaries, empirical handoffs, and unresolved identification requirements for the flagship chain

```text
L -> Phi=sL-K -> accessibility -> invasion -> fixation -> occupancy.
```

## Claim classes

- **THEOREM** — proved within the declared model class.
- **COROLLARY** — follows from an upstream theorem under additional model structure.
- **EMPIRICAL HANDOFF** — quantity may be imported only from an identified empirical analysis.
- **OPEN EMPIRICAL GATE** — required for a biological claim but not yet identified.

## Ledger

| ID | Flagship claim | Class | Canonical source | SLK use | Current ceiling |
|---|---|---|---|---|---|
| C1 | A shared-coordinate conflict budget `L` can be defined once opposing functional geometry is identified on a common fitness scale. | EMPIRICAL HANDOFF | SCH | Entry quantity | `L` must not be inferred from multifunctionality alone. |
| C2 | `L>0` and `Phi<0` define persistent compromise: conflict exists but differentiated architecture is not yet favored. | DEFINITION / CLASSIFICATION | BALANCE + SLK | World classification | Does not by itself establish historical persistence or structural architecture. |
| C3 | For the quadratic partial-release bridge, recoverable conflict loss is `R=sL`. | COROLLARY | BITA | Converts conflict into recoverable architecture value | Model-specific; do not claim arbitrary-landscape universality. |
| C4 | Architecture margin is `Phi=R-K`; under the quadratic bridge `Phi=sL-K`. | DEFINITION + COROLLARY | BITA + SLK | Main crossing coordinate | `K` requires an operational fitness-scale definition. |
| C5 | `Phi>0` means differentiated architecture has higher global optimized payoff than the declared shared comparison, under the registered bridge. | THEOREM / MODEL RESULT | BITA | Global-value criterion | Not yet local accessibility, invasion, fixation, or occupancy. |
| C6 | Global architecture advantage need not imply local mutational accessibility; a finite-jump barrier exists when local marginal release is unfavorable but full release is favorable. | THEOREM / MODEL RESULT | PAYOFF | First realization split after `Phi>0` | Requires declared mutation neighborhood / path geometry. |
| C7 | In frequency-dependent architecture competition, rare-invasion thresholds need not coincide with the intrinsic architecture crossing. | THEOREM / MODEL RESULT | PAYOFF | Separates payoff from invasion | Requires population feedback parameterization. |
| C8 | Finite-population fixation ordering is distinct from deterministic rare invasion. | THEOREM / MODEL RESULT | PAYOFF | Separates invasion from fixation | Process-specific under the declared Moran mapping. |
| C9 | Weak-mutation long-run occupancy is a distinct estimand from local accessibility and pairwise invasion. | THEOREM / MODEL RESULT | PAYOFF | Final flagship transport step | Requires declared mutation graph and fixation kernel. |

## Empirical gates required for a biological application

A complete empirical SLK application requires, in order:

```text
G1  identify a real shared-axis conflict
G2  estimate or bound L on a common fitness scale
G3  define and estimate recoverable release s or R
G4  define and estimate architecture cost K
G5  evaluate Phi
G6  define the local architecture mutation/accessibility neighborhood
G7  estimate population interaction / invasion terms
G8  specify a fixation process if fixation is claimed
G9  specify mutation connectivity if stationary occupancy is claimed.
```

Failure at a later gate does not invalidate an earlier one. The flagship is explicitly cumulative rather than all-or-nothing.

## Anti-overclaim rule

The manuscript must never compress the chain into

```text
Phi>0 -> differentiation evolves.
```

The strongest generic statement is instead

```text
Phi>0 -> differentiated architecture is globally favored under the declared architecture comparison,
```

followed by separate tests of accessibility, invasion, fixation, and occupancy.
