# SCH–BALANCE–SLK–BITA theory programme closure V1

## Purpose

Freeze the final mathematical ownership and claim boundaries across the programme after the SLK split.

## Canonical hierarchy

The architecture-value spine is:

```text
SCH
identify shared-coordinate conflict and export L only after the SCH identification gate
        |
        v
BALANCE
classify/localize the persistent-compromise region
L > 0 and Phi < 0
        |
        v
SLK
Phi = R-K
global architecture value
        |
        v
local accessibility
        |
        v
rare invasion / deterministic population phase
        |
        v
finite-population fixation
        |
        v
weak-mutation occupancy
```

BITA is not a downstream threshold in this chain. It is an orthogonal identification programme:

```text
measured trait interaction
-> compatible mechanism set
-> partial identification
-> selective crossed interventions
-> separability
-> independent remaining-channel evidence
-> mechanism-resolved interpretation
```

## Object-level ownership

### SCH
Owns the identification question required before `L` exists as a downstream quantity:

```text
multifunctionality != identified conflict
state-specific optimum != pure-function optimum
```

### BALANCE
Owns the middle-world geometry:

```text
L > 0
Phi < 0
```

and direct optimized-worldline comparison

```text
Delta_W = W_D* - W_S*.
```

BALANCE may be identified directly from `L>0` and `Delta_W<0` without prior decomposition of `R` or `K`.

### SLK
Owns the architecture-value and evolutionary-realization hierarchy, including ecological threshold displacement derived from that hierarchy.

General architecture margin:

```text
Phi = R-K.
```

Registered quadratic bridge/corollary:

```text
R=sL,
Phi=sL-K.
```

`R=sL` is not promoted to an arbitrary-landscape identity. The ecological formula `E_I-E_V=eta/a` is likewise exact only for the registered affine environmental slice with locally constant `eta`.

### BITA
Owns ecological mechanism identification for an observed trait interaction:

```text
Delta_AD W = rho_delta - iota_delta - kappa_delta
```

as bookkeeping plus identified-set logic. These channel terms are not the same mathematical objects as SLK's `R`, `K`, or `Phi`.

## Closed non-equivalences

Within the declared model classes, the programme has explicit witness regimes for:

```text
L>0
!= Phi>0

Phi>0
!= local accessibility

accessible + Phi>0
!= rare invasion

rare invasion
!= reciprocal fixation superiority

absolute fixation advantage
!= greater weak-mutation occupancy
```

These are existence-of-separation statements, not claims that every adjacent criterion differs in every model.

## Closed invariant

Under connected symmetric rare mutation and the registered exponential Moran process, reciprocal fixation ordering and stationary monomorphic occupancy ordering align through the self-play score difference. Therefore this pair must not be advertised as generically non-equivalent under those assumptions.

## Population-phase separation

For the canonical symmetric frequency-dependent architecture pair,

```text
Delta(p)=Phi+eta(2p-1).
```

The architecture-value crossing `Phi=0` is distinct from rare-invasion boundaries `Phi=±eta`. Hence global value alone does not determine coexistence, bistability, or dominance.

## Direct versus decomposed architecture value

When direct optimized worldlines and the SLK decomposition describe the same context, outcome scale, architecture comparison and modeled channels,

```text
Delta_W = Phi = R-K.
```

Under the quadratic bridge,

```text
Delta_W = sL-K.
```

A non-zero residual

```text
delta_bridge = Delta_W-(R-K)
```

is a bridge failure/mismatch diagnostic until scale, context, cost definition, candidate architecture set and omitted channels are audited. It is not automatically an ecological mechanism, architecture cost, or evolutionary barrier.

## Forbidden compressions

The theory programme is closed against the following shortcuts:

```text
multifunctionality -> conflict
trait interaction -> mechanism
L>0 -> differentiation pays
Phi>0 -> differentiation evolves
invasion -> fixation
fixation ordering -> absolute fixation advantage
absolute fixation advantage -> long-run occupancy
```

Also forbidden:

- using BITA channel residuals as SLK `K` without an independent architecture-cost definition;
- using BALANCE direct reserve as proof of a specific mechanism;
- treating `R=sL` as universal;
- calling `xi` evolutionary time or BALANCE depth a historical transition cost;
- treating observational architecture occupancy as evidence of accessibility/invasion/fixation history.

## Theory closure criterion

The theoretical core is considered closed when the active papers preserve this ownership and no manuscript requires a new theorem merely to connect the existing levels.

Current status:

```text
SCH -> conflict identification             CLOSED as upstream contract
BALANCE -> middle-world geometry           CLOSED as theory object
SLK -> value/realization hierarchy         CLOSED within declared model classes
BITA -> interaction/mechanism identification CLOSED as separate identification theory
```

Remaining work is editorial, empirical, or model-extension work. It is not required to close the current theoretical spine.
